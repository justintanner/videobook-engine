diff --git a/node_modules/videocity-engine/dist/queue/dequeue.js b/node_modules/videocity-engine/dist/queue/dequeue.js
index 993e09e..c5f1c86 100644
--- a/node_modules/videocity-engine/dist/queue/dequeue.js
+++ b/node_modules/videocity-engine/dist/queue/dequeue.js
@@ -35,6 +35,16 @@ export function dequeue(db, pid = process.pid, leaseMs = DEFAULT_LEASE_MS) {
                AND assets.owner_kind = 'provider'
              )
            )
+           AND (
+             pending_jobs.asset_id IS NULL
+             OR NOT EXISTS (
+               SELECT 1
+               FROM pending_jobs AS active_jobs
+               WHERE active_jobs.asset_id = pending_jobs.asset_id
+                 AND active_jobs.id != pending_jobs.id
+                 AND active_jobs.state IN ('running','completing')
+             )
+           )
          ORDER BY pending_jobs.enqueued_at, pending_jobs.id
          LIMIT  1
        )
diff -urN node_modules/videocity-engine.orig/dist/asset/manifest.js node_modules/videocity-engine/dist/asset/manifest.js
--- node_modules/videocity-engine.orig/dist/asset/manifest.js	2026-07-17 05:17:09
+++ node_modules/videocity-engine/dist/asset/manifest.js	2026-07-17 05:22:28
@@ -2,18 +2,15 @@
 import * as path from "node:path";
 import { ok, err, } from "../types.js";
 import { isSafePath, isValidAssetId, invalidInput } from "../validation.js";
+import { resolveAssetDir } from "../file/resolve.js";
 export async function getManifest(projectDir, assetId, options) {
     if (!isSafePath(assetId))
         return invalidInput(`Invalid asset ID: ${assetId}`);
     if (!isValidAssetId(assetId))
         return invalidInput(`Invalid asset ID format: ${assetId}`);
-    const assetDir = path.join(projectDir, assetId);
-    try {
-        await fs.access(assetDir);
-    }
-    catch {
-        return err({ code: "NOT_FOUND", message: `Asset not found: ${assetId}` });
-    }
+    const dirRes = await resolveAssetDir(projectDir, assetId);
+    if (!dirRes.ok) return dirRes;
+    const assetDir = dirRes.value;
     const entries = await fs.readdir(assetDir, { withFileTypes: true });
     const files = [];
     const directories = {};
diff -urN node_modules/videocity-engine.orig/dist/asset/rename.js node_modules/videocity-engine/dist/asset/rename.js
--- node_modules/videocity-engine.orig/dist/asset/rename.js	2026-07-17 05:17:09
+++ node_modules/videocity-engine/dist/asset/rename.js	2026-07-17 05:17:22
@@ -103,6 +103,8 @@
                 db.prepare("UPDATE assets SET asset_id = ? WHERE asset_id = ?").run(result.value.new_asset_id, result.value.old_asset_id);
                 db.prepare("UPDATE pending_tasks SET asset_id = ? WHERE asset_id = ?").run(result.value.new_asset_id, result.value.old_asset_id);
                 db.prepare("UPDATE generation_errors SET asset_id = ? WHERE asset_id = ?").run(result.value.new_asset_id, result.value.old_asset_id);
+                db.prepare("INSERT OR REPLACE INTO asset_aliases (old_asset_id, current_asset_id) VALUES (?, ?)").run(result.value.old_asset_id, result.value.new_asset_id);
+                db.prepare("UPDATE asset_aliases SET current_asset_id = ? WHERE current_asset_id = ?").run(result.value.new_asset_id, result.value.old_asset_id);
             });
             tx();
         }
diff -urN node_modules/videocity-engine.orig/dist/asset/resolver.js node_modules/videocity-engine/dist/asset/resolver.js
--- node_modules/videocity-engine.orig/dist/asset/resolver.js	2026-07-17 05:17:09
+++ node_modules/videocity-engine/dist/asset/resolver.js	2026-07-17 05:17:22
@@ -1,3 +1,4 @@
+import { getStateDb } from "../db/client.js";
 const TAG_PATTERN = /@([a-zA-Z0-9]+-[a-zA-Z0-9_-]+)/g;
 function parseAssetTags(text) {
     const ids = [];
@@ -19,7 +20,23 @@
     for (const tag of tags) {
         // Try exact match first, then prefixed variants
         const candidates = [tag, `img-${tag}`, `vid-${tag}`, `aud-${tag}`, `script-${tag}`];
-        const matchedId = candidates.find((c) => assetIds.has(c));
+        let matchedId = candidates.find((c) => assetIds.has(c));
+        if (!matchedId) {
+            const projectDir = await fs.resolveProjectDir(projectSlug);
+            if (projectDir) {
+                try {
+                    const db = getStateDb(projectDir);
+                    for (const c of candidates) {
+                        const row = db.prepare("SELECT current_asset_id FROM asset_aliases WHERE old_asset_id = ?").get(c);
+                        if (row && assetIds.has(row.current_asset_id)) {
+                            matchedId = row.current_asset_id;
+                            break;
+                        }
+                    }
+                } catch (e) {
+                }
+            }
+        }
         if (!matchedId) {
             unresolved.push(tag);
             continue;
diff -urN node_modules/videocity-engine.orig/dist/db/migrate.js node_modules/videocity-engine/dist/db/migrate.js
--- node_modules/videocity-engine.orig/dist/db/migrate.js	2026-07-17 05:17:09
+++ node_modules/videocity-engine/dist/db/migrate.js	2026-07-17 05:17:22
@@ -3,7 +3,8 @@
 import * as m0002 from "./migrations/0002_pending_tasks.js";
 import * as m0003 from "./migrations/0003_assets.js";
 import * as m0004 from "./migrations/0004_asset_seen_at.js";
-const STATE_MIGRATIONS = [m0001, m0002, m0003, m0004];
+import * as m0005 from "./migrations/0005_asset_aliases.js";
+const STATE_MIGRATIONS = [m0001, m0002, m0003, m0004, m0005];
 function currentVersion(db) {
     const exists = db
         .prepare("SELECT name FROM sqlite_master WHERE type='table' AND name='schema_migrations'")
diff -urN node_modules/videocity-engine.orig/dist/db/migrations/0005_asset_aliases.js node_modules/videocity-engine/dist/db/migrations/0005_asset_aliases.js
--- node_modules/videocity-engine.orig/dist/db/migrations/0005_asset_aliases.js	1970-01-01 07:00:00
+++ node_modules/videocity-engine/dist/db/migrations/0005_asset_aliases.js	2026-07-17 05:17:20
@@ -0,0 +1,10 @@
+export const version = 5;
+export const name = "asset_aliases";
+export function up(db) {
+    db.exec(`
+    CREATE TABLE IF NOT EXISTS asset_aliases (
+      old_asset_id TEXT PRIMARY KEY,
+      current_asset_id TEXT NOT NULL
+    );
+  `);
+}
diff -urN node_modules/videocity-engine.orig/dist/file/read.js node_modules/videocity-engine/dist/file/read.js
--- node_modules/videocity-engine.orig/dist/file/read.js	2026-07-17 05:17:09
+++ node_modules/videocity-engine/dist/file/read.js	2026-07-17 05:21:50
@@ -2,12 +2,15 @@
 import * as path from "node:path";
 import { ok, err } from "../types.js";
 import { isSafeFilename, isSafePath, isWithinDir, invalidInput, } from "../validation.js";
+import { resolveAssetDir } from "./resolve.js";
 export async function readFile(projectDir, assetId, filename) {
     if (!isSafePath(assetId))
         return invalidInput(`Invalid asset ID: ${assetId}`);
     if (!isSafeFilename(filename))
         return invalidInput(`Invalid filename: ${filename}`);
-    const filePath = path.join(projectDir, assetId, filename);
+    const dirRes = await resolveAssetDir(projectDir, assetId);
+    if (!dirRes.ok) return dirRes;
+    const filePath = path.join(dirRes.value, filename);
     if (!isWithinDir(projectDir, filePath))
         return invalidInput("Path escapes project directory");
     try {
diff -urN node_modules/videocity-engine.orig/dist/file/resolve.js node_modules/videocity-engine/dist/file/resolve.js
--- node_modules/videocity-engine.orig/dist/file/resolve.js	2026-07-17 05:17:09
+++ node_modules/videocity-engine/dist/file/resolve.js	2026-07-17 05:19:20
@@ -2,6 +2,7 @@
 import * as path from "node:path";
 import { ok, err } from "../types.js";
 import { isSafePath, isWithinDir, invalidInput } from "../validation.js";
+import { getStateDb } from "../db/client.js";
 export async function resolveAssetDir(projectDir, assetId) {
     if (!isSafePath(assetId))
         return invalidInput(`Invalid asset ID: ${assetId}`);
@@ -12,6 +13,15 @@
         await fs.access(assetDir);
     }
     catch {
+        try {
+            const db = getStateDb(projectDir);
+            const row = db.prepare("SELECT current_asset_id FROM asset_aliases WHERE old_asset_id = ?").get(assetId);
+            if (row && row.current_asset_id) {
+                const newAssetDir = path.join(projectDir, row.current_asset_id);
+                await fs.access(newAssetDir);
+                return ok(path.resolve(newAssetDir));
+            }
+        } catch (e) {}
         return err({ code: "NOT_FOUND", message: `Asset not found: ${assetId}` });
     }
     return ok(path.resolve(assetDir));
